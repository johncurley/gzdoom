<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>English</string>
	<key>CFBundleExecutable</key>
	<string>${MACOSX_BUNDLE_EXECUTABLE_NAME}</string>
	<key>CFBundleIconFile</key>
	<string>zdoom.icns</string>
	<key>CFBundleIdentifier</key>
	<string>org.drdteam.gzdoom</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>GZDoom</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>Development Version</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.action-games</string>
	<!-- Permits programmatic MTLCaptureManager frame capture (the mt_capture
	     console command).

	     CORRECTED 2026-08-17: this comment used to claim the key "only applies
	     when launched as a bundle" and that running Contents/MacOS/gzdoom
	     directly needs METAL_CAPTURE_ENABLED=1 instead. Both halves are false.
	     Executing the binary inside the bundle still resolves the main bundle,
	     so this key applies there too -- measured by the framework's own
	     "Metal GPU Frame Capture Enabled" line appearing on every direct
	     launch, and by unset METAL_CAPTURE_ENABLED having no effect.

	     Set FALSE as of 2026-08-17. With it true the capture layer was active
	     for every run of this build, benchmarks included, instrumenting command
	     buffers and drawables -- a standing contaminant in every measurement
	     this fork has ever recorded. Its in-frame cost measured at 0.00ms
	     (capture_end phase, vid_stalltrace), so this is hygiene rather than a
	     performance fix, but a contaminant that cheap to remove should not be
	     left switched on by default.

	     To take a capture, set METAL_CAPTURE_ENABLED=1 in the environment for
	     that launch -- see docs/gpu-capture-protocol.md. That path is verified;
	     do not assume from this comment alone, since an earlier version of it
	     claimed the key applied only to bundle launches and was wrong. -->
	<key>MetalCaptureEnabled</key>
	<false/>
	<key>LSMinimumSystemVersion</key>
	<string>10.13</string>
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>Doom Resource File</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>CFBundleTypeExtensions</key>
			<array>
				<string>wad</string>
				<string>pk3</string>
				<string>zip</string>
				<string>pk7</string>
				<string>7z</string>
				<string>iwad</string>
				<string>ipk3</string>
				<string>ipk7</string>
			</array>
		</dict>
	</array>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<key>NSSupportsAutomaticGraphicsSwitching</key>
	<true/>
</dict>
</plist>
