Handoff — metal-audit, 2026-08-07

Detail lives in commit messages; this file is open work, protocol, and the
lessons that keep costing money. Tree clean, build green, binary at HEAD.

IF YOU READ NOTHING ELSE: THE COVERAGE HOLE IS CLOSED. Every postprocess pass on
the Metal backend has now been executed and checked on screen at least once.
FIVE were broken — tonemap, lens, colormap, ssao and custom PP. All five are
fixed and confirmed. The standing list no longer contains a single never-tested
pass, for the first time on this branch.

The last four items, all closed 2026-08-07:
  - fxaa PASSED and colormap polarity PASSED (items b and c)
  - Tier 2 direct composite is BLOCKED BY HARDWARE, not by missing work; every
    reachable branch around it passes (item 3)
  - BlitCurrentToImage's V flip VERIFIED, the dead branch executed (item 4)
  - custom PP shaders had FOUR defects and had never run at all (item 5)

CAPTURES ARE NOW UNATTENDED, which is what made that possible. +shotafter and
+execafter arm countdowns from the command line and +cl_capfps 1 makes frames
bit-reproducible, so a control pair at capspot comes back BYTE-IDENTICAL — a
zero noise floor, with nobody at the keyboard. Roughly forty capture launches
were run this session without an operator. See CAPTURE PROTOCOL.

WHAT TO DO NEXT: roadmap priority 3 (texture-upload cold load) also CLOSED
2026-08-07 — measured, not a bottleneck, numbers in AGENTS.md. That leaves
priority 4, the broader compute-postprocess conversion, as the front of the
line, with priority 5 (per-OS optimization, the ARM64 JIT gap) behind it. See
AGENTS.md "Active Roadmap".

Read CLAUDE.md's "Measuring a rendering change", "GPU frame captures" and
"Visual A/B captures" first. Then docs/metal-postprocess-graphs.md, which is new
and is the fastest way to understand the AO chain.

================================================================================
START HERE — nothing is open. The three tests below all PASSED and are kept
as regression checks, each one launch and each unattended.
================================================================================

(0) DONE 2026-08-06 -- AO verified working. Kept as the regression check:

    ./build/gzdoom.app/Contents/MacOS/gzdoom \
      -iwad DOOM2.wad \
      -file ~/Documents/GZDoom/Ashes2063Enriched2_23.pk3 \
      -loadgame capspot.zds \
      +cl_capfps 1 +gl_exposure_speed 1 +gl_ssao 3 \
      +mt_ao_probe 900 \
      > /tmp/aoprobe.log 2>&1

    No console typing -- the probe arms from the command line. The original
    instructions were "type mt_ao_probe, then CLOSE THE CONSOLE and wait",
    which is exactly the operator step the countdown now removes.

    EXPECT: [5] gate passing on most pixels, max ssao.y in the hundreds,
    computed alpha up to ~0.1-0.3; [2] a large non-zero differing-pixel count.
    You should also SEE AO darkening corners for the first time on this branch.

    If [2] is still 0 while alpha is non-zero, fragments are not landing and the
    stencil test comes back into play (mt_postprocess.cpp:269-271).

    This run also exercises the new shader-patch warnings. Any "Metal: shader
    patch MISSED" line in the log is a real finding — grep for it.

(b) COLORMAP — PASSED 2026-08-06. Kept as the regression check.

    RESULT: scene renders greyscale (colormap applied) with the HUD still in
    colour (correct -- PPColormap runs on the scene before 2D), and the frame is
    UPRIGHT. Pre-5a9b53e1b this pass flipped the whole screen, and it fires
    every time the player takes damage, so this closes the only broken-by-
    default path that affected ordinary play.
    Screenshot_Doom_20260806_231319.png.

    POLARITY ALSO CLOSED 2026-08-07. It is a photographic NEGATIVE, as
    InverseMap's ColorizeStart {1,1,1} -> ColorizeEnd {0,0,0} requires, not a
    desaturation. Same viewpoint, one launch each, the sphere delivered by
    +execafter 60 give InvulnerabilitySphere:

      mean scene luminance   39.85 -> 167.75    (a dark interior became bright)
      Pearson r, off vs on   -0.5460
      slope                  -0.630

    The SIGN is the whole test and it is unambiguous: desaturation preserves
    luminance and would give r near +1, so a NEGATIVE correlation can only be
    an inversion. r is not -1 because the HUD is untouched (PPColormap runs on
    the scene only) and the tonemap/exposure curve is nonlinear — both push r
    toward zero, neither can flip its sign. Script at tools/polarity.py.

    NOTE the capture was taken at a different viewpoint, not capspot.zds. Fine
    for an orientation test; not comparable to the AO captures.

    ORIGINAL INSTRUCTIONS follow, for re-running.

    Same launch line, plus +gl_lens 0 +gl_fxaa 0 +gl_tonemap 0.
    Console:  give InvulnerabilitySphere
    Then CLOSE the console and type:  screenshot

    NOT `give invulnerability` (fails in a total conversion) and NOT
    `give PowerInvulnerable` — that has no Powerup.Color, so InitEffect lands on
    NOFIXEDCOLORMAP (powerups.zs:199) and PPColormap::Render early-returns
    before its only Draw (hw_postprocess.cpp:548-551, Draw at :574). It would
    look normal and prove nothing. The InverseMap colour is on the SPHERE
    (doomartifacts.zs:13), which is +INVENTORY.AUTOACTIVATE and lives in base
    gzdoom.pk3, so it works in a TC.

    EXPECT: inverse-greyscale frame, UPRIGHT.

(c) FXAA — PASSED 2026-08-07. Kept as the regression check.

    The first attempt failed because it had no control pair and the frames were
    not deterministic. Both are now fixed by the new shotafter/execafter
    countdown plus +cl_capfps 1, and the whole series runs unattended:

        for cfg in 0 0 1; do
          ./build/gzdoom.app/Contents/MacOS/gzdoom -iwad DOOM2.wad \
            -file ~/Documents/GZDoom/Ashes2063Enriched2_23.pk3 \
            -loadgame capspot.zds \
            +gl_exposure_speed 1 +cl_capfps 1 +gl_fxaa $cfg \
            +shotafter 120 quit
        done

    RESULT, and read the floor first:

      floor (gl_fxaa 0 twice)   BYTE-IDENTICAL, md5 09eb7dd8
      effect (0 vs 1)           4.20% of pixels changed
                                brighter 2.099% / darker 2.100%
                                mean signed delta +0.0044
                                largest block delta +1.371 lum

    That is the prediction met on both halves: the effect clears a zero floor,
    and it is edge-local and symmetric to three decimal places rather than a
    one-directional wash. Contrast the failed first attempt, which was +4.12
    mean signed delta over 39% of the frame — exposure drift, not FXAA. FXAA
    works on Metal, and its even draw count is why it was upright even before
    5a9b53e1b.

    **cl_capfps 1 IS LOAD-BEARING, and this is the reusable part.** Without it
    the same two identical launches diverged by a mean 1.7 lum over 98% of the
    frame with block deltas up to 174 — a floor far larger than FXAA's effect,
    which would have made the result uninterpretable in either direction. The
    cause is that this viewpoint has animated content (the lantern) and the
    tics elapsed by a given RENDERED frame depend on real-world frame rate.
    capfps ties one frame to one tic, and determinism returns exactly.

    Retire the handoff's older claim that the viewpoint "reproduces bit-exactly
    for the same elapsed frame count" as stated without qualification. It is
    true for the same elapsed TIC count; frame count only equals tic count
    under cl_capfps.

================================================================================
THE STANDING LIST
================================================================================

1. AO COMPOSITE — FIXED AND VERIFIED 2026-08-06. Closed.

   Verified three ways, on screen and by two independent instruments:

     mt_ao_probe, before -> after
       mean ssao.x   0.86671 -> 0.86671   (occlusion, correctly unchanged)
       max  ssao.y   0.000   -> 101.375   (depth channel alive; mean 64.861,
                                           sane world units for this interior)
       gate passing  0/278640 -> 278640/278640
       alpha         0 -> mean 0.05532, max 0.21731, non-zero on 59.69% px
       SceneColor    0 differing px -> 550267 (49.43%), maxdelta 0.11230

     Screenshots, gl_ssao 0 vs 3, which were BYTE-IDENTICAL for the life of
     this branch and are not now (md5 d30e0f8e / d3101961):
       mean signed delta -2.0664
       darker  361561 px (50.816%)
       brighter   1057 px (0.149%)      <- 99.7% of change is DARKENING
       largest block delta -15.208 lum at (808,339)
     tools/localize.py shows a coherent dark region fading to no change across
     the right quarter of the frame — occlusion respecting geometry, not a
     global wash. Contrast the FXAA false positive, which was broad
     one-directional BRIGHTENING (exposure). Sign and structure both matter.

     The two instruments agree independently: 49.43% of pixels modified per the
     probe, 50.8% darker per the screenshot pair.

   COST: not established either way, and the a priori argument is that the fix
   is free — it writes centerDepth instead of 0.0 in a shader that already ran,
   and the blend was already executing with zero alpha. Clean metrics after the
   fix read GPU Frame active_avg 15.093ms (min 11.952, max 17.761), 62.8 fps,
   PPCPU AO 0.821ms. Do NOT compare that against the 11.42ms in
   mt_postprocess.cpp:79 — different session, unknown config, and bloom is on
   here at ~2.5ms. A matched measurement needs one launch at ebc854ebf^.

   Note the reference path was paying its full AO cost the whole time it
   contributed nothing.

   PRIOR DIAGNOSIS, kept for the method lesson:

   depthblur.fp's VERTICAL pass wrote 0.0 into the depth channel while the
   horizontal pass preserved it. ssaocombine.fp derives its whole output alpha
   from that channel twice over — depthSignal = 1 - exp2(-ssao.y * 0.01)
   collapses to 0, and the ssao.y > 2.0 gate fails — so the alpha blend was an
   exact no-op. AO was computed correctly and multiplied by zero.

   Measured, not inferred (mt_ao_probe):
       mean ssao.x = 0.86671   <- real occlusion; AO generation was never wrong
       max  ssao.y = 0.000     <- depth channel dead
       gate passes 0 / 278640 px
       SceneColor differing px = 0 / 1113120 across the composite draw

   Fixed in ebc854ebf by preserving centerDepth. NOT a Metal bug: both shaders
   are shared, so GL and Vulkan had it too, from 595ffaf0f. master is unaffected
   — it composites vec4(fogColor, 1.0 - attenutation) with no depth dependency
   at all, so there is nothing to report upstream.

   DECISION: the depth dependency was KEPT rather than reverted to master's
   line. That means AO carries a near-field falloff — depthMask is ~0.29 at 50
   world units, ~0.9 by 400 — so expect weaker close-range AO than stock GZDoom.
   Intended, not a regression. Record any future "AO looks weak up close"
   against this line.

2. STRING PATCHES — CLOSED (91d6e7213). Was "one is silently dead"; the audit
   found five plus a function that could never run. PatchFragmentShader deleted
   (unreachable by construction — it dispatched on names no stock shader has,
   and could only ever fire on a MOD shader whose filename contained "main" or
   "ssao", silently rewriting it). vNormal patch deleted (zero matches).
   ssaocombine alpha patch removed rather than re-derived: it discarded fogColor
   and its *1.85 was tuned against a composite that contributed nothing.
   Everything surviving now warns once per run on a miss or an unexpected match
   count, and every surviving pattern was verified against current sources.

   THE SKY-DOME GUARD WAS RE-DERIVED AND THEN REVERTED THE SAME DAY (66b0b6f64),
   which is the lesson worth keeping. Re-enabling a patch that had matched
   nothing for months is a BEHAVIOUR CHANGE, not a repair — the shader had time
   to evolve around its absence. Measured either side:
       guard off  mean ssao.x = 0.86671   (~13% occlusion)
       guard on   mean ssao.x = 0.99924   (~0.08% occlusion — AO essentially gone)
   Its mechanism is still not understood: it edits only the occlusion expression
   (FragColor.x) yet toggling it also moved FragColor.y from ~65 to ~15600, with
   no compile error logged. Do not reinstate without establishing what it does
   to the translated shader.

   IF ANYONE WANTS THAT ROOT-CAUSED, the discriminating measurement is a
   same-frame, same-pixel readback of raw SceneDepthStencil, LinearDepthTexture.r
   and Ambient0.y together, with zNear/zFar logged. That separates a real
   depth/uniform fault from an SSAO translation/output fault. mt_ao_probe already
   has the readback machinery; it would be a small extension. Nothing depends on
   the answer -- the guard is reverted and AO works -- so this is curiosity, not
   blocking work.

   The depth chain itself is CONFIRMED HEALTHY and needs no change: reverse-Z is
   handled correctly by InverseDepthRangeA=-1/B=1 (hw_postprocess.cpp:839-843),
   and the Metal lineardepth sky patch (1.0 -> 0.0) is correct and properly
   backend-side, since the shared shader's literal 1.0 is right for standard-Z
   GL/Vulkan while Metal's far-plane raw depth is zero. Verified by measurement
   (mean ssao.y 64.861) and by an independent static audit. Default for a long-dead patch is delete or leave
   dead unless you can MEASURE that enabling it helps.

3. TIER 2 DIRECT COMPOSITE — BLOCKED BY HARDWARE, not by missing work
   (2026-08-07). Measured, not assumed: mt_caps reports ReadWrite BGRA8 (Tier2)
   = NO on this Intel HD 6000. Tier 1 read-write covers only r32float/r32uint/
   r32sint, and the scene colour is RGBA16Float, so bloom_combine_rw_all can
   never bind its read_write scene texture here. THIS ITEM AND "Apple Silicon
   untested" ARE THE SAME ITEM — one run on any Tier 2 device settles both.

   Everything reachable around it is now tested and passed:
     composite 1 (force Tier 1)     byte-identical to composite 0   <- correct no-op
     composite 2 (Tier 2 required)  byte-identical to reference PP  <- fallback works
     bloom-off control              differs, 17.1% of px            <- not a dropped effect
   And compute bloom vs reference bloom over the whole frame is now max_delta 1,
   mean 0.049, ZERO pixels differing by more than 2 — the whole-frame
   confirmation of the 2026-08-05 cache fix, which had only ever been checked
   with blob statistics.

   The Tier 2 kernel was read against the verified Tier 1 legs and is
   consistent with them, including the deliberately absent V flip (it never
   leaves texture order, so it needs none). Details in AGENTS.md. Nothing is
   known to be wrong with it; it is simply unexecuted.

4. BlitCurrentToImage's V FLIP — VERIFIED CORRECT 2026-08-07 by mt_wipe_probe.
   The branch is still unreachable in play (CreateWipeTexture always matches the
   pipeline format) but it has now been EXECUTED on hardware, which it never had
   been. Both halves:

     as shipped     r(convert, copy) = +0.9986  r(convert, copy REV) = +0.0967
     flip removed   r(convert, copy) = +0.0967  r(convert, copy REV) = +0.9986

   Same frame in both runs, so that is an exact swap of one pair of numbers,
   not two loosely comparable readings.

   The falsification half is what makes this a result: "AGREE" alone is also
   what a probe that cannot detect inversion would print, and what a silently
   failed blit would print. It also proves the branch runs, which the source's
   own PRINT_LOG marker cannot — PRINT_LOG does not reach stdout, so its absence
   from a redirected log means nothing. Do not read anything into a missing
   PRINT_LOG line.

   The source recipe (force BGRA8Unorm, trigger a wipe, look) was unusable:
   PerformWipe runs to completion inside one D_Display call, so no frame hook
   can catch a frame mid-wipe. Re-run mt_wipe_probe after touching either leg.

5. CUSTOM PP SHADERS — FOUR DEFECTS FOUND AND FIXED 2026-08-07 (25fbdc24b).
   They had never run AT ALL on Metal. No mod download was needed:
   tools/pptest/make.py builds a minimal one designed as a two-sided test.

     python3 tools/pptest/make.py
     ./build/gzdoom.app/Contents/MacOS/gzdoom -iwad DOOM2.wad \
       -file ~/Documents/GZDoom/Ashes2063Enriched2_23.pk3 -file /tmp/pptest.pk3 \
       -loadgame capspot.zds +cl_capfps 1 \
       +pptest_scale 1.0 +execafter 60 shaderenable pptest 1 +shotafter 120 quit

   At scale 1.0 the shader is an identity and the frame MUST be byte-identical
   to no shader -- which proves the pass runs, is exact, and is upright all at
   once. At 0.5 it must differ, and that half is what makes the first mean
   anything. Verified on all three targets; details in AGENTS.md.

   THE STANDING LIST IS NOW EMPTY of never-tested passes.

6. TEXTURE UPLOAD — CLOSED 2026-08-07, measured and NOT a bottleneck.
   Steady state is exactly zero uploads. A cold load is ONE frame: 54 uploads,
   9.6ms, 55 command buffers on a map transition, i.e. ~0.15ms per new texture
   and exactly one command buffer each. It lands inside a transition the player
   already waits through. The TextureUploadCPU timer fired for the first time,
   which also confirms the 2026-08-04 async-path fix.

   Drive it unattended with +mt_debug 1 and +execafter 150 map map01, and read
   the per-frame lines -- mt_metrics' 120-frame rolling average is the WRONG
   instrument and washes the burst out.

   One reading in the first run showed a 46.75ms stall on the burst frame and
   did NOT reproduce (3.3ms in two further runs). It was first-run cost, not the
   uploads. Repeating it cost one launch and would have cost a session.

7. NEW STANDING TRAP: PRINT_LOG DOES NOT REACH STDOUT. The engine printed the
   exact glslang error behind the custom-shader failure all along, and every
   redirected launch log looked clean. It also nearly caused a wrong reading in
   the wipe probe the same day. Use +logfile before concluding anything is
   silent, and never read a missing PRINT_LOG line as evidence. Metal shader
   parse/link failures and missing shader lumps are now PRINT_HIGH in red.

================================================================================
WHAT LANDED TODAY (2026-08-07)
================================================================================

8 commits. Build green throughout, tree clean, binary at HEAD. The previous
session's 19 commits are in git log; they are not repeated here.

  Instruments and capture infrastructure
    627febacb  shotafter/execafter -- unattended, deterministic capture series
    3703c6c24  execafter queues instead of replacing (found within the hour)
    ea0ab7f43  mt_wipe_probe, and BlitCurrentToImage's V flip VERIFIED

  Fixes
    25fbdc24b  custom PP shaders: four defects, the pass had never run at all
               (3 in shader compilation, 1 a target Metal never dispatched;
                shader failures now PRINT_HIGH instead of invisible)

  Findings that closed items without a code change
    4276a4de7  Tier 2 direct composite is blocked by hardware; all reachable
               branches around it pass, and compute-vs-reference bloom is now
               whole-frame identical (max_delta 1)

  Documentation and corrections
    2f0a76633  fxaa and colormap polarity both PASS
    c39f9f240  correct the determinism claim -- it is per TIC, not per frame
    c5a899d19  custom PP write-up; PRINT_LOG promoted to a standing trap

Two claims this file had been carrying were WRONG and are corrected in place:
the viewpoint reproduces per tic and not per rendered frame (cl_capfps is
load-bearing), and "Tier 2 has never executed" was a hardware ceiling rather
than an untested path.

REGRESSION CHECKS RUN AT SESSION END, after the shader-compilation changes,
which touch EVERY Metal PP shader and not just custom ones:
  - default config at capspot, post-fix vs pre-fix baseline:  max_delta 0
  - gl_tonemap 4 (sqrt(c*c) identity) vs gl_tonemap 0:        max_delta 0
  - gl_tonemap 1 vs 0, the necessary other half:              86.7% of px
Both halves each time. An identity that matches proves nothing on its own.

Commit messages carry the reasoning. This file deliberately does not repeat it.

================================================================================
THE COVERAGE HOLE — still the durable finding
================================================================================

This branch is validated by "build it, load a WAD, look at it", so the tested
surface equals exactly whatever the dev config happens to enable. Passes behind
a default-off cvar had never had a single pixel checked. Four were broken.

Assume every default-off postprocess setting is untested until someone turns it
on and looks.

  pass        gate                     default        draws  status
  present     always                   on             1      exercised constantly
  exposure    with bloom               off (fresh)    many    on in this ini
  bloom       gl_bloom                 false          many    on in this ini; sub-1%
  tonemap     gl_tonemap               0 = off        1      WAS BROKEN -> fixed
  lens        gl_lens                  false          1      WAS BROKEN -> fixed
  fxaa        gl_fxaa                  0 = off        2      PASSED (was fine)
  ssao        gl_ssao                  0 = off        many   FIXED, verify with (0)
  colormap    GAMEPLAY (fixedcm/flash) fires in play  1      FIXED, passed + polarity
  menu blur   gl_menu_blur / gameinfo  -1 = mod's     many   runs, output sane
  custom PP   gl_custompost + a mod    true           N      WAS TOTALLY BROKEN -> fixed

FINAL TALLY: of the ten passes above, FIVE were broken and none of the breakage
was visible in review. tonemap, lens and colormap were inverted (one root cause,
three passes); ssao composited nothing; custom PP never ran at all (four
separate defects in one pass). fxaa, bloom, exposure, present and menu blur were
fine. The prediction the table encoded — that a pass behind a default-off cvar
has never had a single pixel checked — held every time it was tested.

"Default" means "in this ini" for bloom, exposure and ssao; in a FRESH config
only present runs. Cvar defaults and draw counts verified 2026-08-06
(hw_postprocess_cvars.cpp:30,41,47,53,61,99; hw_postprocess.cpp:997; fxaa 2
draws at :380/:392, lens 1 at :350, tonemap 1 at :632, colormap 1 at :565).

Gates the table omits, any of which could confound a future A/B: bloom also
needs the default fixed colormap, no SSAO debug mode, and positive scene
dimensions (:78); tonemap can be forced on by a LEVEL OVERRIDE at gl_tonemap 0
(:610); ssao also returns early on zero dimensions or zero effective strength
(:788).

**colormap is the one that matters and it is not a cvar at all.** A palette
flash — taking damage, picking up an item — makes it run one draw. Pre-5a9b53e1b
that means the screen flipped upside down every time the player got hit. It
survived unnoticed because all testing happens standing still at a pinned
savegame where nothing ever flashes.

================================================================================
TOOLS
================================================================================

- mt_ao_probe [frames] (NEW) — reports the AO composite numerically: the blend
  style actually handed to PSO creation, SceneColor read back before AND after
  the draw with a differing-pixel count, the stencil/clear decisions, whole-frame
  SceneFog stats, and the shader's own alpha derived from Ambient0. Needs
  gl_ssao != 0 and gl_ssao_debug 0.
- mt_wipe_probe [frames] (NEW) — runs BOTH legs of BlitCurrentToImage on the
  same pipeline image and compares their vertical luminance profiles, forwards
  and reversed. Reports its own discriminating power, so a symmetric source
  cannot produce a confident wrong answer.
- mt_capture [frames] (NEW) — programmatic Metal GPU frame capture, no Xcode
  build needed. Needs METAL_CAPTURE_ENABLED=1 on the launch. See
  docs/gpu-capture-protocol.md. Encoder labels are greppable straight out of the
  trace: `strings -n 6 <trace>/capture | grep '^PP '`.
- mt_bloom_dump [frames] — per-level pyramid stats plus PNGs. Dumps only the path
  that ACTUALLY RAN last frame; comparing compute against reference takes two
  launches.
- tools/tonemap_predict.py — predicts the Reinhard capture from the Uncharted2
  one by inverting the curves. Checks tonemap modes by curve RELATION, not by
  difference.
- tools/polarity.py (NEW) — inversion vs desaturation, by the SIGN of the
  luminance correlation. Closes the class of test where the two outcomes look
  the same by eye.
- tools/blobstats.py, pngdiff.py, localize.py, cluster.py as before. Each
  docstring encodes rules that cost a session.

================================================================================
CAPTURE PROTOCOL — screenshots
================================================================================

0. **+cl_capfps 1 ON EVERY LAUNCH IN A SERIES.** Without it the noise floor at
   capspot is a mean 1.7 lum over 98% of the frame, which is larger than a real
   FXAA effect. Animated content advances by TIC, and how many tics have
   elapsed by rendered frame N depends on the frame rate. capfps pins one frame
   to one tic and the floor collapses to byte-identical.
1. Pin the viewpoint with capspot.zds (MAP51 lantern).
2. PUT `+execafter <n> mt_caps` IN EVERY CAPTURE LAUNCH. It prints the
   RESOLVED path and the live cvar values into the log, which turns "which
   config was this file?" from inference into a line you can grep — and it is
   what caught a whole five-launch matrix silently running one config. In zsh,
   an unquoted variable holding several arguments does NOT word-split; use
   ${=VAR}, or the engine receives one argv entry and only its first cvar
   applies.
   ONE LAUNCH PER CONFIGURATION, cvars on the command line, and take the shot
   with `+shotafter <frames> quit` so the operator types NOTHING. If the capture
   needs a game state rather than a cvar, deliver it with
   `+execafter <frames> <command>` at a lower count — that is how the colormap
   sphere is given. Both countdowns run only on in-level frames, so the count
   means the same thing in every launch of the series.
3. Never chain console commands with `;`.
4. The console pauses the game and GetScreenshotBuffer only re-presents, so a
   shot taken with the console open reflects the last frame rendered with it
   CLOSED. macOS Cmd+Shift+4 sidesteps this by capturing the live window.
5. -exec runs before `Init complete.` and +screenshot never fires.
6. +mt_caps on the command line prints viewport numbers from before the first
   frame. The format line is valid; the viewport lines are not.

Determinism: this viewpoint reproduces BIT-EXACTLY for the same elapsed TIC
count — which equals the frame count only under cl_capfps 1, see item 0. The
older unqualified "same frame count" phrasing was wrong and cost one capture
round. The asymmetry is
the point:
  - BYTE-IDENTICAL is strong: no drift occurred AND the change did nothing.
  - BYTE-DIFFERENT is AMBIGUOUS: equally consistent with a real effect and with
    exposure drift. It needs a same-config control pair before it means
    anything. Measured counterexample: gl_fxaa 0 vs 1 gave +4.12 mean signed
    delta over 39% of the frame, which was exposure, not FXAA.
The tic-vs-frame explanation is no longer a plausible mechanism but a measured
one: identical launches diverge without cl_capfps and are byte-identical with
it.

================================================================================
THE METHOD LESSONS — the expensive ones
================================================================================

- **A DEBUG MODE THAT CHANGES THE PIPELINE IS NOT AN OBSERVATION OF THE
  PIPELINE.** This is the big one from today, and it cost four sessions.
  hw_postprocess.cpp:910 wraps both AO blur passes in `if (gl_ssao_debug < 2)`,
  so every debug mode that displays depth SKIPS the pass that destroys it. Four
  correct debug-mode readings all pointed away from the bug. Before trusting a
  debug view, check what it changes besides the display.
- **A test whose two outcomes look the same is not a test.** Recurred twice
  today: `give PowerInvulnerable` would have passed a colormap test that never
  ran, and a probe reading sampled the darkest pixels — where fog and scene are
  both black and "equal" is guaranteed. Both were caught by asking what the
  negative result would look like.
- **Match the minimum, not the sentence.** Every dead string patch died because
  it spelled out a whole statement and someone edited part of it — including our
  own commits. Anchor on the smallest thing that must be true.
- **Verify the artifact you changed is the one being loaded.** There are TWO
  gzdoom.pk3 files; the engine loads the one in the app bundle. Updating
  build/gzdoom.pk3 alone left the engine running a six-week-old shader and made
  a correct fix measure as a total failure. Also clear the MSL cache.
- **Measure the magnitude, do not eyeball the image.** gl_ssao_debug 3 looked
  "near-black, so depth must be normalised" — the obvious reading, and wrong.
- **A mathematical identity is the strongest available probe.** gl_tonemap 4 is
  sqrt(c*c) == c, which removed shader maths as a variable entirely.
- **Byte-identical captures are ambiguous, ALWAYS.** They mean "perfect
  identity" and "the pass never ran" equally well.
- **State a numeric prediction before looking.** Every real defect on this branch
  was caught this way; several confident readings of the code were not.
- **Check gzdoom.ini before diagnosing a renderer bug.** An entire AO
  investigation once resolved to config.

================================================================================
RUNNING THE GAME
================================================================================

    ./build/gzdoom.app/Contents/MacOS/gzdoom \
      -iwad DOOM2.wad \
      -file ~/Documents/GZDoom/Ashes2063Enriched2_23.pk3 \
      -loadgame capspot.zds \
      +gl_exposure_speed 1

- -loadgame takes a BARE filename. capspot.zds lives in
  ~/Documents/GZDoom/Savegames/doom.id.doom2.commercial/, OUTSIDE the repo and
  NOT covered by git. Do not delete it; it is what makes captures comparable
  across builds.
- Redirect stdout to a log. That log proves which config a screenshot belongs to.
- Engine screenshots land in ~/Documents/GZDoom/Screenshots. Use these rather
  than Cmd+Shift+4 for anything involving pixel maths.
- After ANY change under wadsrc/:
      build/tools/zipdir/zipdir -udf \
        build/gzdoom.app/Contents/MacOS/gzdoom.pk3 wadsrc/static
      rm -f ~/Library/Application\ Support/zdoom/cache/*.msl
  Note the BUNDLE path. See the method lesson above.
- Frames render at only a few fps at this viewpoint. Budget over a minute for
  anything armed for N frames.

Config at session end: gl_tonemap=0, gl_lens=false, gl_fxaa=0, gl_ssao=3,
gl_bloom=true, gl_bloom_amount=1.4, gl_exposure_base=0.35, gl_exposure_speed=1,
mt_compute_bloom=false, mt_compute_ao=false, mt_hdr_pipeline=true,
vid_preferbackend=3, r_drawfuzz=1.
Window 1440x801, scene viewport 1440x773.
